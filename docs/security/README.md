# Security

Security architecture and implementation guidance for Ivadoo belong here.

The specification requires HTTPS, secrets outside source code, secure password storage, revocable sessions, 2FA, role-based access control, company and establishment scopes, immutable auditing for sensitive operations, encrypted backups and files, strict organization isolation, configurable retention and inter-tenant leakage tests.

Security documentation must distinguish product requirements from country-specific legal compliance.
