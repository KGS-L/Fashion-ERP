# Environments

Ivadoo uses distinct environments for development, test, staging and production.

Each environment must keep its own configuration, secrets, data boundaries and operational procedures. Production data must not be reused casually in lower environments.

Environment-specific implementation details will be added progressively as deployment choices are validated.
