# Object storage

Ivadoo stores documents and binary files in S3-compatible object storage or MinIO, with metadata kept in PostgreSQL.

This area will contain storage configuration and operational documentation for photos, sketches, technical sheets, contracts, invoices, certificates and other managed files.