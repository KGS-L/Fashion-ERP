# Audit

Provides the audit trail for sensitive operations.

Audit records must capture the user, date, affected object and relevant previous and new values so important business transitions remain traceable.
