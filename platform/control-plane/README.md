# Control Plane

The Control Plane manages FashionERP platform-level concerns such as organizations, licenses, subscriptions, modules, installations, versions and support.

It must remain separate from customer business data and must not copy sensitive Data Plane information by default.