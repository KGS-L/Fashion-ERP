# Identity migrations

Versioned Django schema migrations for the FashionERP identity foundation live in this directory.

The initial migration creates the project-owned UUID user model and the revocable API session table.
