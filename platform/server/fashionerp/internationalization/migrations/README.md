# Internationalization migrations

Versioned Django migrations for currencies, dated exchange rates and configurable units live in this directory.

Company/User/Establishment preference fields are versioned in their owning Django applications while depending on the internationalization schema where required.
