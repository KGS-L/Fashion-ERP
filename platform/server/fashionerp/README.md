# Django project package

This package contains the server-level Django configuration for FashionERP.

It owns project settings, root URL routing and ASGI/WSGI entrypoints. Business-domain code must remain modular and must not be accumulated in this package.
