# Quality migrations

Versioned Django schema migrations for the Phase 3 quality domain.

Migrations create the inspection, criterion, defect and rework structures plus their scoped quality permissions. Historical migrations are immutable after merge; schema evolution must use a new migration.
