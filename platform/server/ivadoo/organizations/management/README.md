# Organization management commands

Provisioning commands in this package operate on the local ERP Data Plane database.

They are administrative/bootstrap operations and are not exposed as ordinary tenant API endpoints.
