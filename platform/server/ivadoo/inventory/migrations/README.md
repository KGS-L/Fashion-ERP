# Inventory migrations

Versioned PostgreSQL migrations for the Ivadoo Phase 3 inventory domain.

Stock movement immutability is enforced both by application behavior and a PostgreSQL trigger. Quantity constraints are database-backed.
