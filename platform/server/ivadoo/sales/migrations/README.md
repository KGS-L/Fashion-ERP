# Sales migrations

Versioned schema migrations for Phase 2 sales and Phase 3 fitting/alteration/customer-validation extensions.

Historical migrations remain immutable after merge; later schema evolution must use a new migration.
