# Delivery migrations

Versioned Django migrations for the Phase 3 delivery workflow.

Schema changes create deliveries, order-line quantities, packages, immutable proof records and scoped delivery permissions. Historical migrations are not edited after merge.
